import { BreakLineAfterPeriodPipe } from './break-line-after-period.pipe';

describe('BreakLineAfterPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new BreakLineAfterPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
